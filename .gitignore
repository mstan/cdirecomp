# Build trees
/build/
build/

# Generated C (throwaway artifacts — regenerate from the disc + recompiler)
hotelmario/generated/*.c
hotelmario/generated/*.h
bios/generated/*.c
bios/generated/*.h
bios/generated/*.log

# Disc images / ROMs (large, not ours to redistribute)
*.bin
*.cue
*.iso
*.rom

# CeDImu is an optional local-only development oracle (MC-CDI-017).
# Never commit, submodule, package, or publish its tree/local modifications.
/external/CeDImu/

# Python bytecode
__pycache__/
*.pyc

# Editor / OS noise
*.obj
*.o
*.exe
*.pdb
.vs/
.vscode/
*.user
Thumbs.db
